import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    header: {
        alignItems: "center",
        justifyContent: "center",
        height: 100,
        flexDirection: "row",
        gap: 8,
    },
    logo: {
        width: 48,
        height: 48,
        marginTop: 20,
    },
    text: {
        fontSize: 24,
        fontWeight: "bold",
        color: "#F2F2F2",
        marginTop: 20,
    },
});
